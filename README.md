This project is a Streamlit application that performs emotion classification on text using a pre-trained machine learning model.

Steps to Set Up the Project
1. Set Up a Virtual Environment
To create a virtual environment for the project, run the following commands based on your system:

For Windows:
bash
Copy
Edit
python -m venv venv
For macOS/Linux:
bash
Copy
Edit
python3 -m venv venv
2. Activate the Virtual Environment
For Windows:
bash
Copy
Edit
.\venv\Scripts\activate
For macOS/Linux:
bash
Copy
Edit
source venv/bin/activate
3. Install Dependencies
After activating the virtual environment, install the required packages from the requirements.txt file:

bash
Copy
Edit
pip install -r requirements.txt
4. Run the Streamlit App
Once all dependencies are installed, you can start the app using Streamlit:

bash
Copy
Edit
streamlit run app.py
This will open the app in your default web browser, and you can start interacting with the Emotion Classifier.
